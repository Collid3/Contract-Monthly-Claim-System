@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<Claim>

<table>
    <thead>
        <tr>
            <th>ClaimID</th>
            <th>Amount</th>
            <th>Description</th>
            <th>Date</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @{
            foreach (var claim in Model) {
                <tr>
                    <td>@claim.Id</td>
                    <td>@(@claim.HourlyRate * @claim.HoursWorked)</td>
                    <td>@claim.AdditionalNotes</td>
                    <td>@claim.SubmissionDate</td>
                    <td>@claim.Status</td>
                    <td>
                        <a asp-action="verifyClaim" asp-route-id="@claim.Id">Verify</a>
                        <a asp-action="rejectClaim" asp-route-id="@claim.Id">Reject</a>
                    </td>
                </tr>
            }
        }
    </tbody>

</table>